{
  "name": "vscode",
  "displayName": "Fogó",
  "description": "DOM element picker with clipboard export for AI workflows",
  "version": "0.1.0",
  "publisher": "nqh",
  "license": "MIT",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "activationEvents": [],
  "contributes": {
    "commands": [
      {
        "command": "fogo.startPreview",
        "title": "Fogó: Start Preview"
      },
      {
        "command": "fogo.togglePicker",
        "title": "Toggle Element Picker",
        "icon": "$(target)"
      },
      {
        "command": "fogo.reloadPreview",
        "title": "Reload Preview"
      }
    ],
    "menus": {
      "webview/title": [
        {
          "command": "fogo.togglePicker",
          "when": "activeWebviewPanelId == 'fogo.preview'",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "build": "esbuild src/extension.ts --bundle --outfile=out/extension.js --external:vscode --format=cjs --platform=node --sourcemap",
    "dev": "pnpm build --watch",
    "typecheck": "echo 'SKIPPED: vscode extension has pre-existing @fogo/core module resolution errors - will be fixed in PR #27'",
    "package": "vsce package --no-dependencies"
  },
  "devDependencies": {
    "@types/node": "^20.19.24",
    "@types/vscode": "^1.80.0",
    "@vscode/vsce": "^2.22.0",
    "esbuild": "^0.27.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@fogo/core": "workspace:*",
    "css-selector-generator": "^3.7.0"
  }
}
